# IP-Adressen, die die Ticket-API erreichen dürfen.
# Lokal: nur dieser Rechner. Produktion: hier die Adresse(n) des Hilfecenters eintragen,
# z. B. "allow 203.0.113.17;" — und die beiden Zeilen für localhost entfernen.
allow 127.0.0.1;
allow ::1;
deny  all;
